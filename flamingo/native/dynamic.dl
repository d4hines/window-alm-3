import json

typedef OID = s64

output relation Window(id: OID, width: s64, height: s64)

Window(0, 100, 100).

output relation S(s: Result<string, string>)

S(s) :- w in Window(),
    var s = to_json_string(w).
